server.port=80
server.servlet.context-path=/system-svc/api/v1

#JDBC Configuration
#spring.datasource.driver-class-name=org.postgresql.Driver
#spring.datasource.url=jdbc:postgresql://localhost:5432/estate
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.url=jdbc:h2:mem:test
spring.jpa.hibernate.ddl-auto=create
#spring.datasource.url=jdbc:sqlserver://anthonytam.database.windows.net:1433;database=estatedb;encrypt=true;trustServerCertificate=false;hostNameInCertificate=*.database.windows.net;loginTimeout=30;
#spring.datasource.username=anthonytam
#spring.datasource.password=HKBNP@ssw0rd0202
spring.liquibase.change-log=classpath:db/changelog/db-changelog.json
spring.jpa.properties.hibernate.jdbc.time_zone=UTC
spring.datasource.hikari.pool-name=msaPool
spring.datasource.hikari.auto-commit=false
spring.datasource.hikari.connection-timeout=30000
spring.datasource.hikari.maximum-pool-size=50
spring.datasource.hikari.max-lifetime=1800000
spring.datasource.hikari.minimum-idle=2

# i18n properties
# Whether to always apply the MessageFormat rules, parsing even messages without arguments.
spring.messages.always-use-message-format=false
# Comma-separated list of basenames
spring.messages.basename=messages
# Loaded resource bundle files cache duration.
# When not set, bundles are cached forever.
# If a duration suffix is not specified, seconds will be used.
spring.messages.cache-duration=
# Message bundles encoding.
spring.messages.encoding=UTF-8
# Whether to fall back to the system Locale
# if no files for a specific Locale have been found.
spring.messages.fallback-to-system-locale=true
# Whether to use the message code as the default message instead of throwing a "NoSuchMessageException".
# Recommended during development only.
spring.messages.use-code-as-default-message=false

# Swagger
springdoc.api-docs.path=/api-docs